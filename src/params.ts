// User roles
export const roles = {
    admin: 1,
    coordinador: 2,
    asesor: 3,
    docente: 4
};  

// Error codes
export const codeErrors = {
    duplicatedKey: 1062, // register duplicated key
};

// General system params
export const params = {
    storage: 'factory',
}